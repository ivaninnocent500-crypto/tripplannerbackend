# Copy to .env for local development. Never commit the real .env.

# LEGACY database — now used ONLY for GenerationLog (operational/audit
# data). This is your existing ati-production Postgres/SQLite, unchanged.
DATABASE_URL=postgresql://user:password@localhost:5432/ati_production
# DATABASE_URL=sqlite:///./ati_dev.db

# NEW: Supabase Travel Intelligence knowledge base connection string.
# ⚠️ STILL PENDING as of this delivery — every engine will raise a clear
# RuntimeError at request time until this is set. See app/db/session.py's
# docstring for the port 6543 (pooled, NullPool) vs port 5432 (direct)
# distinction — confirm which your Supabase project uses before setting
# this, since guessing wrong causes real connection failures.
#
# Format: postgresql://postgres.[project-ref]:[password]@[host]:[port]/postgres
SUPABASE_DATABASE_URL="postgresql://postgres.fphaxcfimqusdobdjxpf:[CHANDIKA123ivan]@aws-1-us-west-1.pooler.supabase.com:5432/postgres"

# Shared API key the Android app sends as the X-Api-Key header.
ATI_API_KEY=ati_sec_98f3a71b2940bc62e08

# AI Gateway configuration.
GEMINI_API_KEY=
ATI_AI_ENABLED=true
